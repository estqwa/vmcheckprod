# 📚 Документация Админ-панели Trivia

## 🔐 Как войти в админку

### Требования:
1. Зарегистрироваться/войти с аккаунтом **с ID = 1** (первый зарегистрированный пользователь)
2. Перейти по адресу: `/admin`

### Почему ID = 1?
В текущей версии бэкенда администратором считается пользователь с `user.id === 1`. Это первый зарегистрированный пользователь в системе.

---

## 📍 Страницы админки

### 1. Dashboard (`/admin`)
**Главная страница админки**

| Элемент | Описание |
|---------|----------|
| Список викторин | Все викторины отсортированы по дате (новые сверху) |
| Status Badge | `scheduled` (синий), `in_progress` (зелёный), `completed` (серый), `cancelled` (красный) |
| "+ Create Quiz" | Кнопка создания новой викторины |
| "← Back to App" | Вернуться в приложение игрока |

**Функции:**
- Просмотр всех викторин
- Клик по викторине → переход к деталям

---

### 2. Создание викторины (`/admin/quizzes/new`)
**Форма создания новой викторины**

| Поле | Обязательное | Описание |
|------|--------------|----------|
| Title | ✅ Да | Название (3-100 символов) |
| Description | Нет | Описание (до 500 символов) |
| Scheduled Time | ✅ Да | Дата и время старта |

**Валидация:**
- Время должно быть в будущем
- После создания → переход к редактированию вопросов

---

### 3. Детали викторины (`/admin/quizzes/[id]`)
**Полное управление викториной**

#### Информация:
- Название и статус
- Описание
- Дата создания и запланированного старта
- Количество вопросов

#### Действия (кнопки):

| Кнопка | Когда доступна | Что делает |
|--------|----------------|------------|
| **+ Add Questions** | Статус `scheduled` | Открывает форму добавления вопросов |
| **Reschedule** | Статус `scheduled` + есть вопросы | Изменить время старта |
| **Cancel Quiz** | Статус `scheduled` | Отменить викторину |
| **Duplicate** | Есть вопросы | Скопировать викторину с новым временем |

#### Добавление вопросов:

| Поле | Описание |
|------|----------|
| Question Text | Текст вопроса |
| Option 1-4 | 4 варианта ответа |
| Correct Option | Номер правильного ответа (0-3) |
| Time Limit | Время на ответ (5-60 сек) |
| Points | Очки за правильный ответ |

**Валидация:**
- Минимум 2 варианта ответа
- Текст вопроса обязателен

---

## 🎮 Полный цикл создания викторины

```
1. /admin → "+ Create Quiz"
   ↓
2. Заполнить: Title, Description, Scheduled Time
   ↓
3. Click "Create Quiz" → переход к /admin/quizzes/[id]
   ↓
4. Click "+ Add Questions"
   ↓
5. Добавить вопросы (текст, варианты, правильный ответ)
   ↓
6. Click "Save Questions"
   ↓
7. Викторина готова! Запустится автоматически в указанное время.
```

---

## ⚙️ Технические детали

### Защита админки:
```tsx
<ProtectedRoute requireAdmin>
  <AdminComponent />
</ProtectedRoute>
```

Компонент `ProtectedRoute` проверяет:
1. `isAuthenticated` — пользователь залогинен
2. `isAdmin` — `user.id === 1`

Если условия не выполнены → редирект на `/login` или `/`

### API эндпоинты админа:
| Endpoint | Метод | Назначение |
|----------|-------|------------|
| `/api/quizzes` | POST | Создать викторину |
| `/api/quizzes/:id/questions` | POST | Добавить вопросы |
| `/api/quizzes/:id/schedule` | PUT | Изменить время |
| `/api/quizzes/:id/cancel` | PUT | Отменить |
| `/api/quizzes/:id/duplicate` | POST | Дублировать |

Все требуют: `Auth + Admin + CSRF`

---

## 🔧 Конфигурация

**Переменные окружения (`.env.local`):**
```env
NEXT_PUBLIC_API_URL=https://your-backend-url.com
NEXT_PUBLIC_WS_URL=wss://your-backend-url.com
```

Обе переменные используют `NEXT_PUBLIC_` префикс, чтобы быть доступными в браузере.

---

## 📱 Адаптивность

Админка полностью адаптивна:
- Desktop: полная ширина форм
- Tablet: responsive cards
- Mobile: stacked layout

---

## ❓ FAQ

**Q: Не вижу кнопку Admin Panel?**  
A: Убедитесь, что ваш user.id === 1. Кнопка появляется на странице Profile.

**Q: Как изменить админа?**  
A: В текущей версии это хардкод. Для изменения нужно модифицировать бэкенд или AuthProvider.

**Q: Можно ли редактировать уже добавленные вопросы?**  
A: В текущей MVP версии — нет. Можно только добавлять новые.

**Q: Викторина не запускается в указанное время?**  
A: Убедитесь, что бэкенд запущен и есть хотя бы 1 вопрос.


 Для деплоя нужно:
Создать .env.production:

NEXT_PUBLIC_API_URL=https://your-production-backend.com
NEXT_PUBLIC_WS_URL=wss://your-production-backend.com
Запустить: pnpm run build && pnpm run start

Или задеплоить на Vercel/другой хостинг

